interface:
  display_name: "GCONF Insight Extract"
  short_description: "Извлечение инсайтов из базы GCONF"
  default_prompt: "Use $gconf-insight-extract to turn newly ingested GCONF evidence into human-reviewable semantic cards with exact source quotes and case provenance."
policy:
  allow_implicit_invocation: true
